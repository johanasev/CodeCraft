# ===== CODECRAFT - VARIABLES DE ENTORNO =====
# Copia este archivo a .env y configura los valores según tu entorno

# ===== BASE DE DATOS =====
DB_NAME=codecraft_db
DB_USER=codecraft
# CAMBIAR EN PRODUCCIÓN - Usar password seguro (mínimo 16 caracteres)
DB_PASSWORD=secure_password123
# CAMBIAR EN PRODUCCIÓN - Usar password seguro (mínimo 16 caracteres)
DB_ROOT_PASSWORD=root_password123

# ===== DJANGO BACKEND =====
# GENERAR NUEVA SECRET_KEY EN PRODUCCIÓN:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-change-this-in-production-1234567890

# DEBUG: True para desarrollo, False para producción
DEBUG=True

# ALLOWED_HOSTS: Dominios/IPs permitidos (separados por comas)
# Ejemplo desarrollo: localhost,127.0.0.1
# Ejemplo producción: tudominio.com,tu-ip-vps,www.tudominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS: Orígenes permitidos para peticiones (separados por comas)
# Ejemplo desarrollo: http://localhost:5173,http://localhost
# Ejemplo producción: http://tudominio.com,https://tudominio.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost

# ===== USUARIO ADMINISTRADOR INICIAL =====
# Se crea automáticamente al iniciar el backend por primera vez
# CAMBIAR EN PRODUCCIÓN
DJANGO_SUPERUSER_EMAIL=admin@codecraft.com
DJANGO_SUPERUSER_PASSWORD=admin123

# ===== FRONTEND =====
# URL del backend para peticiones API
# Ejemplo desarrollo: http://localhost:8000
# Ejemplo producción: http://tudominio.com:8000 o https://tudominio.com/api
VITE_API_URL=http://localhost:8000

# ===== REDIS (OPCIONAL - para caché en producción) =====
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=redis_password_seguro

# ===== CONFIGURACIÓN DE EMAIL (OPCIONAL - para notificaciones) =====
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-password-de-aplicacion

# ===== NOTAS IMPORTANTES =====
# 1. NUNCA subas el archivo .env a Git (ya está en .gitignore)
# 2. SIEMPRE usa passwords fuertes en producción
# 3. SIEMPRE genera una nueva SECRET_KEY en producción
# 4. SIEMPRE configura DEBUG=False en producción
# 5. Configura ALLOWED_HOSTS con tu dominio/IP real en producción
# 6. Usa HTTPS en producción (configura SSL con Nginx/Let's Encrypt)